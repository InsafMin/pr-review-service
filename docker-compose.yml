services:
  postgres:
    image: postgres:15-alpine
    container_name: pr-review-postgres
    environment:
      POSTGRES_USER: ${DB_USER:-prservice_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-prservice_password}
      POSTGRES_DB: ${DB_NAME:-prservice}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./migrations/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "${DB_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-prservice_user} -d ${DB_NAME:-prservice}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pr-review-service
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: ${DB_USER:-prservice_user}
      DB_PASSWORD: ${DB_PASSWORD:-prservice_password}
      DB_NAME: ${DB_NAME:-prservice}
      SERVER_PORT: 8080
      LOG_LEVEL: ${LOG_LEVEL:-info}
    ports:
      - "${SERVER_PORT:-8080}:8080"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 20s
    restart: unless-stopped

volumes:
  postgres_data:
    driver: local